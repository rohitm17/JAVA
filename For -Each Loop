import java.util.*;
class Feach{
	public static void main(String args[]){
	Scanner s= new Scanner(System.in);
	System.out.println("Enter the no of elements of array");
	int n=s.nextInt();
	int arr[]=new int[n];
	System.out.println("Enter elements");
	for(int i=0;i<n;i++){
		arr[i]=s.nextInt();
	}
	int total=0;
	//for-each loop
	//for(data_type variable : array | collection)
	for(int x:arr){
		total=total+x;

	}
	System.out.println("SUM IS:"+total);
	}
}
/*The advantage of the for-each loop is that it eliminates the possibility of bugs and makes the code more readable.
 It is known as the for-each loop because it traverses each element one by one.
Drawback of the enhanced for loop or for-each loop is that it cannot traverse the elements in reverse order.
*/
